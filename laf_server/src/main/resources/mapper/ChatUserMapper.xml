<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.laf.mapper.ChatUserMapper">

    <!-- 根据用户名查询用户信息 -->
    <select id="pageQuery" resultType="Long">
        SELECT toId FROM chat_user WHERE fromId = #{id}
    </select>

</mapper>